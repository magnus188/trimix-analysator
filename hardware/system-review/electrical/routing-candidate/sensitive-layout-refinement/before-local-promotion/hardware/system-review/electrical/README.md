# Electrical review — prototype engineering record

The complete routed main board passes native KiCad with **zero violations, zero unconnected items and zero schematic mismatches**. The reviewed source has been promoted into the canonical project, preserving the previous draft. Its [final delivery index](main-final/README.md) and [frozen review bundle](routing-candidate/final-cleanup/frozen-bundle/review/geometry-handoff-manifest.json) bind board SHA256 `9f274fdf198f1bcb871a4fc360e76693f8a5b048553e6a02998141737956514f`. Independent final CAM is complete; source-bound enclosure integration is being finalized. **It is not ready to order.**

`hardware/pcb/analyzer/` is the promoted routed project. Root-level netlist, qualification, pinmap, height and native reports now match it. `main-final/` contains the final CAM, assembly lists, factory paste subset and selected-hole manufacturing plan. `hardware/pcb/usb-input/` is the separately frozen daughterboard and was not changed by promotion. Immutable baselines and rejected trials are retained.

## Corrections implemented

- U401/U502 remain ADS122C04IPWR; AO2, JJ-CCR and MD62 sensors are unchanged. Helium is physically **HE_REF minus HE_SENSE**. R506/R507 are680ohm0.1%; He acquisition profile2/O2profile1.
- U501/U702 now TPS7A2030PDBVR with correct pin4NC. C503/C706 are DNP. Selected effective capacitance exceeds the documented0.47uF output requirement under the stated engineering deratings.
- U302 LM66100 supplies HOST_5V and blocks reverse current toward our VOUT_5V converter. This does not establish every Guition USB/battery backfeed path.
- BQ D+/D- remain NC, preventing its autonomous HVDCP/MAXC voltage negotiation. TUSB320LAI detects USB-C advertisements; PI3USB9201 detects legacy BC1.2. Daughterboard harness is six conductors. The old discrete Rd R901/R902 have been removed because the sink controller supplies dead-battery Rd. The USB board has dedicated four-line signal ESD and VBUS ESD parts; their clamping does not establish survival of every downstream surge.
- The unsupported4.02k BQ ILIM extrapolation was rejected: BQ specifies no ILIM operation below500mA. U114 is the exact **TPS22950CQDDCRQ1** leaded Q1 variant, with sourced19.1k0.1% default resistor and qualified parallel1k branch. Its manufacturer34/50/66mA table applies at19.2k; the19.1k substitution has only a typical-formula estimate, with current corners and transient overshoot still unmeasured. BQ R103300ohm is a supported-range backup.
- U112 authorization latch plus two series FETs require a fresh host edge after VBUS/CC/fault reset. U113 senses the protected input independently of firmware. Q112 exposes actual latch reset through shared GPIO49/CHG_INT_N. GPIO52 stays unused.
- U115 TPS259470ARPWR adds sustained-input OVLO with fixed precision resistors. Its AUXOFF, not FLT, clears permission. Physical fast-surge/ESD/inrush qualification remains open; the6V-absolute downstream limiter is not proven to survive every pulse.
- Critical capacitor packages/banks and inductors were replaced with exact sourced parts and maximum-height envelopes. Optional SW101 was replaced by local QON service pad TP1014, referenced to TP1001 GND; the external power button is unchanged. J402 is the specified Amphenol142138 SMB jack with a manufacturer-drawing reconstruction; original connector poses remain fixed.

## Evidence and limits

The final source-validation audit has 217 passing pin-function/net assertions and bounded calculations. `main-drc.json` and the frozen bundle's `review/drc.json` identify the complete routing check. The routing audit passes 12 checks, all 301 routed-via interfaces have explicit dispositions, and cleanup conserved all 195 physical pad-connectivity partitions. Independent CAM passed 44 export/connectivity checks, 20 assembly-ledger checks and 50 process checks, retaining one external supplier-qualification hold. Native and CAM checks do not establish physical qualification. The STEP and 153-row component-height table must be read together; generic or absent visual models are not purchased-part maximum envelopes.

The current raw ERC report contains one reviewed U302 ST-to-ground error. TI explicitly permits grounding the unused LM66100 status output. The [exact exception guard](erc-reviewed-exceptions/README.md) checks the specific pins, complete schematic wiring, native netlist, library identities and captured report without changing pin types or suppressing the error class. Its result means one reviewed raw error and no unexpected violations, not a clean raw report or a fabrication release. Refresh this guard alongside the final native ERC export.

Earlier placement snapshots are historical. The final bundle includes the inward keyed J301, rotated output-capacitor bank and CO inductor, true 0402 components, compact backside OVP cluster, local service pads and corrected C708 ground dogbone. Final source-matched enclosure integration is pending. In1 remains reserved for continuous ground; local In2 signal routing and anchored ground partitions have separate review evidence. The published JLC04161H-3313 stackup is bound in the native board; the actual fabrication quote remains pending.

The root `hardware/system-review/` report reconciles firmware and CAD checks. Analogue sweeps, capacitor curves, USB protection research, and failed/successful simulation receipts are separate evidence, not physical measurements. The TPS22950 PSpice experiment failed its light-load sanity check; it is **not** a device-performance pass.

## Before ordering

Complete independent exported-file and final enclosure review; confirm the fabricator stackup and factory assembly process for exact lands, three signal VIPPO sites and thermal vias; validate actual mechanical mates and harness envelopes; establish cell identity/current/temperature limits and real pack NTC; qualify unresolved protection/current/transient assumptions. Stock, price and assembly quotations are not confirmed. No physical test is marked performed. The bundle includes 169-reference assembly and marking maps, with exact purchasing, factory, manual, DNP and PCB-feature lists. The full-board paste export is diagnostic; the factory subset stencil still needs assembler approval.

The optional internal charger LED D101/R106 was removed to make room for C108, a supplementary 4.7 µF local SYS bypass. Both qualified C104/C105 bulk capacitors remain. TP1013 exposes charger STAT on the rear copper; remove the board for probing. TP1014 retains manual QON wake/reset access close to U101. The local service pads use the assembly reference drawing where dense silk would be unreadable. Probe fit and physical fault recovery remain pending. See `charger-bypass-change.json` and `qon-service-change.json`.

C101 now uses an actual KEMET C0603C105K4RACTU 1 µF, 16 V, X7R 0603 part. This retains the nominal 1 µF BQ input-bypass function while allowing a short front-copper connection beside U101. C102 remains the selected 22 µF 1210 PMID bulk capacitor. The local source change is recorded in `local-input-cap-change.json`; `routing-candidate/local-cap-loop-witnesses.json` records explicit copper paths, without claiming measured ripple, inductance or thermal performance. `standard-part-selections.json` is refreshed from the exported schematic by `refresh_selection_snapshot.py`, so removed LED/switch parts and the superseded Harwin host header do not remain in the active selection snapshot.
