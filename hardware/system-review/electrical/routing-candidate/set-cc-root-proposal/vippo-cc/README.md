# Protection-chip CC escape — isolated routing proof

**Local checks passed; the combined board is still unfinished and must not be ordered.** The frozen candidate is [native-v3/Trimix_Analyzer.kicad_pcb](native-v3/Trimix_Analyzer.kicad_pcb), SHA-256 `d490110c6335a6594a8d2171386926c65a95e0f8ba729feb378c478e8a676a63`.

One 0.40/0.20 mm epoxy-filled, copper-capped via at U115 pin 3 replaces the old remote via and three rear-copper escape segments. A single 0.15 mm front trace connects it to the retained destination via. The existing voltage-setting circuit and resistor values remain; no R129 jumper or new signal net is needed. The existing rear SET trace starts 0.035 mm lower within its original solder land to maintain ordinary routing clearance to both neighboring features.

Native KiCad checks report **no CC/SET geometry errors and no schematic-parity findings**. Five explicit pad-to-pad witnesses pass. All 22 CC conductor objects form one native connected group; all 11 SET objects form one group. The candidate still has three unfinished connections and four geometry findings from the separately identified C116/charger-interrupt clash. Silkscreen and dangling-copper findings also remain. See [native-v3-drc.json](native-v3-drc.json) and the [guarded delta and connectivity evidence](guarded-delta-and-witnesses.json).

The special via needs the exact local rules in the candidate project. Its copper extension is masked; the original U115 pad still defines the rear solder-mask and paste aperture. Independent inspection of the actual Gerber and drill bytes passed 25 scoped checks, including failing controls for missing rules, a larger or moved via, and an incorrectly opened mask. [Independent manufacturing-export review](../../../vippo-process-review/native-v3-independent/README.md). Published process capability does not replace the factory's selected-hole, flatness, assembly and quotation acceptance.

The ground comparison refilled the original copper under the same project settings before measuring it. In1 remains one connected region with 105 contacting ground anchors. In2 remains eleven regions with 21 contacting anchors; every region reaches In1. No anchor was lost. Its physical copper area increased by 5.349 mm²; In1 increased by 0.093 mm². The two actual-copper views were visually inspected. These are geometric contact checks, not current, thermal or noise measurements. [Ground evidence](ground-review/review.json).

For integration, validate each original raw item in [copper-patch.kicad_sexpr](copper-patch.kicad_sexpr) before changing it. A source that already contains the separately frozen three-segment rear destination route must retain those existing segments rather than duplicate them. Apply only the missing via/front escape, the four source removals and the SET endpoint correction, then refill and repeat combined DRC, connectivity and ground checks.

`native/` and `native-v2/` retain failed clearance trials. The R129 and fixed-corridor searches in the parent folder are rejected diagnostics. Only `native-v3/` is this scoped handoff. The authoritative project, final PCB STEP and enclosure fit have not been updated by this work.
