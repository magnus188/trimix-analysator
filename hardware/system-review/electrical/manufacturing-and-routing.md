# Manufacturing and routing contract — NOT RELEASED

Target process: JLCPCB four-layer1.6mm main PCB, nominal1oz outer copper, ENIG; separate two-layer0.60mm GCT daughterboard. The native main board binds the published JLC04161H-3313 stackup:35µm outer copper,15.2µm inner copper,0.0994mm prepreg each side and1.265mm core. Published layer thicknesses sum to1.5642mm; nominal finished board thickness remains1.6mm, with the supplier's finished-thickness tolerance kept separate. See [stackup source and native binding](stackup-review.md). Vendor confirmation in the actual quote, board tolerances, panel/support carrier and assembly process remain pending. No fabrication files should be purchased from this draft.

Layer intent: F.Cu components/short sensitive traces and power loops; In1.Cu continuous ground return; In2.Cu allocated power/distribution and carefully reviewed remaining signals, with individually anchored ground regions beneath the backside OVP cluster, tied by vias to continuous In1; B.Cu additional routing and any explicitly CAD-qualified low-profile parts. Do not split analogue return paths or let switch-node copper pass beneath high-impedance sensor signals. Power, heater and digital return currents must return through broad ground to the source without sharing long sensor-return traces.

Keep AO2/JJ-CCR raw differential inputs short, shielded by continuous ground and away from switching nodes. Preserve SMB shell as differential negative. Keep each ADC input filter local and symmetric. Review ADC reference/supply decoupling and I2C settling against the actual harness. No arbitrary global autorouter output qualifies noise or thermal performance.

Power placement/routing requirements: BQ PMID cap/ground, SW/inductor/VSYS cap, BAT cap andREGN/BTST must form short loops. TPS63020 input/output banks and both inductor pins require broad paths with short pad necks. Follow L101's central trace/via keepout; copper pour is allowed only as its manufacturer drawing states. TPS61023 switch path must be compact. Input source-limiter and OVP divider/slew capacitor must stay local. A placement that fits courtyards but scatters these passives into the analogue area is rejected.

Main default routed copper clearance remains0.20mm and board-edge clearance0.50mm. Only same-package manufacturer pad-to-pad gaps at U701/U703/U801/U111/U112 have explicit0.15mm exceptions. Those do not authorize arbitrary traces at smaller clearance. Fine package escape and thermal pad processes must be reviewed individually. U101/U201 thermal0.20mm drill features need factory fill/cap acceptance if retained; do not use open vias in paste and assume hand soldering is safe. JLC published capability is evidence of a process option, not a production quote.

Ordinary signals use 0.15 mm traces and 0.50/0.25 mm through vias. Explicit short U111 pin escapes use 0.125 mm traces, and the listed dense U101/U110/U115 escape points use 0.45/0.20 mm through vias. Both routed via sizes retain 0.125 mm nominal annulus; power distribution uses separately reviewed larger vias. The native rules retain 0.20 mm electrical clearance. `audit_routing_geometry.py` checks every sub-0.15 mm track against the U111 rectangle and every sub-0.25 mm routed drill against the explicit coordinate list, preventing the net-scoped KiCad exceptions from silently becoming broad permissions. All thermal drilling and fabrication acceptance remain separate.

Exact RPW0010A U115 land is asymmetric, with two central IN/OUT lands and no center-ground pad. Its stencil/paste and same-number compound lands require CAM/assembly inspection. U114 uses exact leaded DDC6 Q1 lands; the older WCSP part and industrial C substitutes are superseded. Do not replace its part with a similarly named500mA-minimum switch.

Use native DRC, schematic parity, net counts, copper connectivity, visual layer checks and current/thermal calculations after every geometry/routing change. Unconnected items remain failures to complete routing even when geometric violations are zero. Board STEP exports only establish nominal geometry; use the maximum-height contract and actual harness models too.

Hybrid assembly: factory reflow for QFN/HotRod/exposed-pad parts, matched network and fine-pitch ADCs; larger SMT/THT may be populated by the user with microscope, iron and hot air after inspection. Produce exact grouped BOM/CPL and agree populated/DNP lists with the factory. BOM standard candidates are sourced but stock and substitutions are not approved by this review. Print a1:1 pad/part fit sheet before release.

The frozen main candidate is now fully connected with zero native DRC findings and zero schematic mismatches; see `routing-candidate/sensitive-layout-refinement/frozen-local-bundle/review/geometry-handoff-manifest.json`. It has 2247 track segments and 300 routed vias. The exact U115.3 CC escape at (13.6,89.775) uses a 0.40/0.20 mm filled/capped via with 0.10 mm nominal annulus and a coordinate/pin-scoped 0.12 mm B-copper exception. Actual neighbour gap is 0.125 mm. Only this reviewed site has that annulus/clearance exception; original SMT mask/paste aperture is retained, extension masked, front tented. U115.1 at (13.6,91) and R125.1 at (17.95,87.925) use separately declared 0.50/0.25 mm VIPPO. All three require resin fill, copper cap on both faces, flatness and assembler approval. Ordinary open vias are not substitutes. The manufacturer-capability review is in `vippo-process-review/`; the exact quote remains a release hold.

Final digital receipts include 126 independent CAM checks, 29 preserved power/monitor witnesses, 35 connected local witnesses, continuous In1 with 107 ground anchors, and 11 anchored In2 regions with 21 anchors. The source-matched CAD disposition is bound in the promotion manifest; actual cable/part fit remains a physical qualification. Dynamic current/thermal/ripple/EMI testing remains separate from digital routing completion. The raw ERC report retains one precisely guarded TI-permitted LM66100 ST-to-ground exception. Gerber and assembly uploads are not authorized purchase actions in this review.

The frozen assembly ledgers separate 27 factory components, 118 manual components, 6 DNPs and 18 PCB/harness features. All 169 references have a value/MPN/coordinate map; 141 have visible reference silk. The factory list includes ICs, RN501, actual 0402 parts and Q110's manufacturer-land implementation. The native full-board paste output includes manual/DNP apertures for export-equivalence review and **must not be used as an approved factory stencil**. The exact 27-part factory-only aperture subset passed independent geometry comparison; obtain assembler approval before ordering.

The BQ load-current switch path stays on F.Cu. A single coordinate-scoped 0.20 mm B.Cu C103 bootstrap branch runs from (15.08,80.7) to the ordinary 0.50/0.25 mm via at (15.75,81.35); it is the only permitted rear BQ_SW branch. Three negative controls confirm that the routing validator rejects an altered endpoint, altered width or move onto the inner reference layer. No general switch-net exception is granted.

C103 and R702 are true 0402 factory parts; C107 is the actual 47 µF/10 V TDK 0805 part with manufacturer-derived reflow lands and belongs in factory assembly. R504 remains the same 0603 manual part, now on the rear, and must be installed before the carrier. Use the current [reference and assembly lists](main-final/README.md) and [marked-inductor orientation supplement](main-final/assembly-notes.md). All 24 package thermal bores plus the three signal VIPPO sites require resin fill and copper cap on both faces. Their exact 27-hole list and factory-only paste subset still need supplier flatness, process, stencil and two-sided reflow approval; published capability alone does not qualify them.
