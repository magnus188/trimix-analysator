# Deprecated KiCad import — archive only

This directory contains the original 2026-09-05 EasyEDA-to-KiCad migration.
Its power circuits are outdated and its PCB is empty, so none of these files
are authoritative for the current analyzer.

Use the active projects instead:

- [Main analyzer](../../analyzer/Trimix_Analyzer.kicad_pro)
- [USB input daughterboard](../../usb-input/Trimix_USB_Input.kicad_pro)
- [PCB overview](../../README.md)

The archived workspace is `Trimix.kicad_pro`. `easyeda-source/` contains the
original `.epro` package and initial import archive, while `verification/`
contains the migration/ERC evidence. See [migration notes](MIGRATION_NOTES.md)
for the original conversion record.
