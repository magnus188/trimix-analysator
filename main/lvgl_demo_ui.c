// Demo UI removed. File kept intentionally empty as a stub to avoid build
// references if any lingering includes exist. Safe to delete entirely.

// (Previously contained example_lvgl_demo_ui implementation.)
