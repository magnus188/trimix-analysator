dependencies:
  lvgl/lvgl:
    version: "9.3.0"
    rules:
      - if: "target == esp32s3"

  espressif/esp_lcd_touch_gt911:
    version: "1.1.3"
    rules:
      - if: "target == esp32s3"

  idf:
    version: ">=5.1,<5.2"
